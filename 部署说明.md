# 爬虫网站部署说明（另一台设备使用）

## 一、环境要求
- Node.js 20+（推荐 24，包内用 24.18 开发）
- npm（随 Node 自带）

## 二、安装
```bash
npm install
npx prisma generate
```

## 三、数据库
- 已包含 `prisma/dev.db`：账号、RAG 知识库（14 条印刷辅料知识）、爬虫历史、客户数据全部在内，**直接可用，无需迁移**
- 数据库已做多账号隔离（会话/知识/配置按用户归属）
- 备份文件 `prisma/dev.db.bak-ownership` 可删除

## 四、配置 .env（已随包提供）
- `AUTH_SECRET`：登录加密密钥（跨设备保持一致，改了就全部重新登录）
- `CRAWLER_PROXY="http://127.0.0.1:10808"`：海外爬虫代理（VPN/代理软件地址，**必须改为新机器的代理端口**）
- `OPENAI_MODEL="deepseek-chat"` 等 AI 配置：AI 功能需要各自账号在「设置」页配 API Key（已隔离按账号存储）

## 五、启动
开发模式：
```bash
npm run dev
```
生产模式（推荐）：
```bash
npm run build
npm run start
```
访问 http://localhost:3000

## 六、账号（三个都可用）
- admin@supplyai.com / Admin123（管理员，可见全部数据）
- vendor@supplyai.com / Admin123
- vendor2@supplyai.com / Admin123
> 密码已 bcrypt 哈希存储；连续 5 次失败锁 15 分钟（指数递增）；改密后所有旧会话失效

## 七、公网部署
本机运行 + Cloudflare Tunnel 暴露：
1. 下载 cloudflared：https://github.com/cloudflare/cloudflared/releases （windows-amd64.exe）
2. 启动：`cloudflared tunnel --url http://localhost:3000`
3. 输出里的 `https://xxx.trycloudflare.com` 即公网地址（临时，重启变化）
4. 正式使用建议配自有域名（命名隧道）

## 八、爬虫功能注意
- 海外采集（TradeWheel/DDG/Bing/客户）必须开启 VPN/代理，且 .env 的 CRAWLER_PROXY 指向代理端口，否则海外源会超时/403
- 国内厂商采集（1688 等）直连
- 无头浏览器使用本机已安装的 Edge/Chrome
- 爬虫中心采集结果和配置均按账号隔离

## 九、常见问题
- 登录报错：浏览器清 cookie 或无痕窗口（旧 cookie 会导致 error=undefined）
- 数据库改动前先备份 dev.db
- 端口占用：改 `npm run start -- -p 3001` 或先杀旧进程